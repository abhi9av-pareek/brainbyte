{
  "name": "gyantra-root",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd Backend && npm run dev",
    "frontend": "cd signin-signup_frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "@react-oauth/google": "^0.13.5",
    "cors": "^2.8.6",
    "node-fetch": "^3.3.2"
  }
}
